# Oneness Site Prompt Library

Each section below can be used as a standalone prompt for an AI assistant tasked with producing UI designs, copy, assets, or backend integrations for the specified page. All experiences are for a Japanese audience; ensure copy is written in natural Japanese unless otherwise requested.

---

## Landing Page (`/` – `src/app/page.tsx`)
**Prompt Goal**  
You are designing the Oneness Kingdom landing page. Build a warm, aspirational single-page experience for Japanese visitors curious about joining a spiritually aligned digital kingdom. Maintain the existing section structure: hero banner with immersive background image, call-to-action button leading to /register, feature grid with four highlight cards, "How to start" three-step process, mission statement, OP制度 explanation with multiple cards (basic info, points systems, titles, exchange rules), and a concluding community posts teaser. Use imagery hints that match kawaii, harmonious, semi-futuristic aesthetics. Emphasize love, peace, sustainability, and community contribution. Keep typography elegant with headline accents, ensure responsive layout, and write all headings/bodies in natural Japanese.

**Existing UI Text**
- Hero
  - "ワンネスキングダムへようこそ"
  - "私たちの王国に参加する"
- Features Section
  - "特徴"
  - "ワンネスキングダムが提供する価値や体験を、わかりやすくご紹介します。"
  - "貢献とつながり"
  - "あなたの愛、学び、貢献の行動は、私たちのコミュニティ内で価値として視覚化され、循環します。"
  - "行動がポイントに変換され、王国経済に参加"
  - "貢献が可視化され、コミュニティから評価"
  - "持続可能な価値循環システム"
  - "ヒューマンネットワークの構築"
  - "フォロー、評価、推薦を通じて有意義な関係を築き、デジタルな家族の絆さえも形成します。"
  - "AI支援のマッチングシステム"
  - "信頼スコアに基づく推薦"
  - "グローバルなつながり"
  - "AIを活用したコミュニティ"
  - "私たちのプラットフォームは、公正なマッチング、推薦、そして私たちの王国の安全と調和を確保するためにAIを使用しています。"
  - "不正行為の自動検出"
  - "パーソナライズされた推薦"
  - "コミュニティガイドラインの自動執行"
- Values
  - "ワンネス王国の価値観"
  - "愛"
  - "すべての行動の基盤となる無条件の愛"
  - "平和"
  - "対話と理解を通じた調和の促進"
  - "持続可能性"
  - "長期的な繁栄を考慮したシステム設計"
- Getting Started
  - "ワンネスキングダムの始め方"
  - "シンプルな3つのステップで、あなたの旅がスタートします。"
  - "アカウントを登録"
  - "あなたのプロフィールを作成し、ワンネスへの旅を始めましょう。"
  - "メールアドレスで簡単登録"
  - "プロフィール設定"
  - "初期ポイント付与"
  - "AIによる認証"
  - "高度なAI強化認証プロセスであなたの身元を保護します。"
  - "顔認証による本人確認"
  - "行動パターン分析"
  - "継続的な信頼度評価"
  - "コミュニティに参加"
  - "貢献し、つながり、私たちの王国の成長に参加しましょう。"
  - "投稿と交流"
  - "ポイント獲得"
  - "経済活動への参加"
  - "始める準備はできましたか？"
  - "今すぐ登録"
- Mission Section
  - "愛と貢献のメタソーシャルプラットフォーム"
  - "私たちは、価値が経済力や軍事力ではなく、愛とつながりによって定義される新しい国際コミュニティ国家を構築しています。"
  - "私たちの使命"
  - "デジタル空間に調和のとれた王国を創造する"
  - "個人の貢献を可視化し、正当に評価する"
  - "AIを活用した公平なコミュニティ運営"
  - "国境を越えた真のつながりを促進"
  - "創設者"
  - "ワンネスキングダムは、テクノロジーとスピリチュアリティの融合を信じるグローバルチームによって設立されました。"
  - "技術基盤"
  - "ブロックチェーン、AI、クラウド技術を組み合わせ、透明性と安全性を確保しています。"
- OP System Section
  - "🪙 OP制度"
  - "愛・平和・調和・貢献を基準に、会員が提供・共有・成長を通してポイント（価値）を循環させる仕組みです。"
  - "基本理念"
  - "💱 基本情報"
  - "通貨名: OP"
  - "換算レート: 1 OP = 100円"
  - "会員登録: 無料"
  - "最終目標: 「王様の王様」= 真のワンネス達成者"
  - "✨ 7つの軸"
  - "愛" / "知恵" / "公正" / "力" / "超能力" / "治療" / "審判"
  - "🎯 基本活動ポイント"
  - "提供登録（AI審査通過）"
  - "紹介成功"
  - "会員同士が繋がる"
  - "寄付を行う"
  - "❤️ 家族制度ポイント"
  - "夫・妻になる"
  - "夫婦になる"
  - "子供になる"
  - "兄弟・姉妹になる"
  - "🌟 コミュニティ制度"
  - "コミュニティ世話役"
  - "コミュニティ参加"
  - "コミュニティに寄付"
  - "コミュニティ招待"
  - "🏆 社会貢献制度"
  - "教育者になる"
  - "生産者になる"
  - "仲介役"
  - "平和維持セキュリティ役"
  - "👑 称号制度"
  - "賢者"
  - "君"
  - "王様"
  - "🌟 最高称号: 王様の王様 - 1,000,000 OP 🌟"
  - "💱 仮想通貨交換ルール"
  - "交換上限: 月1回、保有OPの最大33％まで"
  - "対象者: 7軸いずれかの称号者（賢者以上）"
  - "交換可能通貨: JPYC・TEC"

---

## Login Page (`/login` – `src/app/login/page.tsx`)
**Prompt Goal**  
Create the Oneness Kingdom login page experience. Provide a two-column layout: left pane hosts the login card (logo, welcome back heading in Japanese, short reassurance copy, dynamic login form component placeholder). Right pane is a full-height inspirational photo sourced from PlaceHolderImages (use data-ai-hint metadata to choose appropriate imagery) with a top-to-bottom gradient overlay. Include a link back to registration. Ensure tone is gentle, inviting returning members to reconnect. All instructions for interactive elements should note dynamic import of LoginForm.

**Existing UI Text**
- "おかえりなさい"
- "アカウントにアクセスするには資格情報を入力してください"
- "アカウントをお持ちでないですか？"
- "こちらで登録"

---

## Register Page (`/register` – `src/app/register/page.tsx`)
**Prompt Goal**  
Design the onboarding entry page that wraps the multi-step registration form. Similar two-column layout as login: left column introduces the kingdom, contains logo, hero message, supportive copy, and dynamically loaded RegisterForm component. Right column displays the register hero image with gradient overlay. Copy should inspire newcomers to begin their harmony journey. Include link back to login.

**Existing UI Text**
- Wrapper Page
  - "王国に参加する"
  - "アカウントを作成して、調和への旅を始めましょう。"
  - "すでにアカウントをお持ちですか？"
  - "こちらでログイン"
- `RegisterForm` Component Highlights
  - "王国への招待"
  - "まずは基本情報を入力してください"
  - "表示名"
  - "コミュニティ内で表示される名前です"
  - "メールアドレス"
  - "パスワード"
  - "最低6文字以上である必要があります。"
  - "次へ: アバター作成 ✨"
  - "アバターを作成しましょう"
  - "現在アバターを生成中です。完了後にこのページが表示されます。"
  - "生成されたアバターを確認して次のステップに進みましょう。"
  - "アバターを生成すると自動的に次のステップに進みます。"
  - "次へ: プロフィール入力へ進む"
  - "生成ツールが表示されない場合は こちら をクリックして別ウィンドウで開いてください。"
  - "戻る"
  - "プロフィールを完成させましょう"
  - "あなたの興味や価値観を教えてください"
  - "アバターを生成している間にプロフィールを入力できます。"
  - "アバター生成が完了すると自動的にアバター確認ページに戻ります。"
  - "登録が完了しました"
  - "登録が完了しました。ダッシュボードにリダイレクト中..."
  - "王国へようこそ！"

---

## Forgot Password Page (`/forgot-password` – `src/app/forgot-password/page.tsx`)
**Prompt Goal**  
Produce a gentle password reset request page. Focus on a centered card layout with Oneness logo, headline reassuring the user in Japanese, brief explanatory paragraph, email input form (React Hook Form with zod validation), submit button with loading state, and success confirmation screen instructions. Copy must guide users clearly through requesting a reset, with notes about checking spam folders. Provide separate states for initial form and success confirmation.

**Existing UI Text**
- Success State
  - "メールをご確認ください"
  - "パスワードリセット用のリンクをメールで送信しました。"
  - "メール内のリンクをクリックしてパスワードをリセットしてください。"
  - "※ メールが届かない場合は、迷惑メールフォルダもご確認ください。"
  - "ログインに戻る"
- Form State
  - "Reset your password"
  - "Enter your email address and we'll send you a link to reset your password."
  - "Email"
  - "Enter your email"
  - "Sending reset link..."
  - "Send reset link"
  - "Back to login"
- Toast Copy
  - "リセットメールを送信しました"
  - "送信に失敗しました"
  - "エラー"
  - "エラーが発生しました。メールアドレスを確認してもう一度お試しください。"
  - "予期せぬエラーが発生しました。しばらくしてからもう一度お試しください。"

---

## Reset Password Page (`/reset-password` – `src/app/reset-password/page.tsx`)
**Prompt Goal**  
Craft the new password setup experience the user reaches from the email link. Include suspense loading state, error state for invalid/expired token, successful password update confirmation, and main form state with two password fields (new + confirm), validation messaging, and CTA button with spinner. Remind user that copy is in Japanese and that Supabase handles the token via URL hash. Mention automatic redirect to /login after success.

**Existing UI Text**
- Error State
  - "Invalid reset link"
  - "This password reset link is invalid or has expired."
  - "Request new reset link"
- Success State
  - "Password reset successful"
  - "Your password has been updated. Redirecting to login..."
  - "Sign in now"
- Main Form
  - "Set new password"
  - "Enter your new password below."
  - "New Password"
  - "Enter new password"
  - "Confirm Password"
  - "Confirm new password"
  - "Updating password..."
  - "Update password"
  - "Back to login"
- Toast Copy
  - "Password updated"
  - "Reset failed"
  - "Error"
  - "Your password has been successfully reset."
  - "An unexpected error occurred."

---

## Auth Callback (`/auth/callback` – `src/app/auth/callback/page.tsx`)
**Prompt Goal**  
Document the server-side auth callback flow for Supabase OAuth. The prompt should instruct an AI engineer to ensure session retrieval, profile existence checks, profile creation fallback with default banner and welcome points, AI avatar state initialization, and secure httpOnly cookie storage (access_token, refresh_token). Describe redirect paths for success (/dashboard) and failures (/login with query params). Emphasize secure handling of service role key and no user-facing UI.

**Existing UI Text**
- (Server-side route with no direct UI copy; focus on redirects and Supabase integration.)

---

## Kawaii Generator Page (`/kawaii-generator` – `src/app/kawaii-generator/page.tsx`)
**Prompt Goal**  
Generate a full-page layout that solely showcases the KawaiiGenerator component outside the registration flow. Center the content within a white background, include a headline and short description about using the generator independently, and allow generous spacing so the component feels primary. Tone should be playful yet clear, encouraging experimentation.

**Existing UI Text**
- Wrapper Page
  - "カワイイアバター ジェネレーター"
  - "メインの登録フローでジェネレーターが表示されない場合でも、ここから直接利用できます。"
- `KawaiiGenerator` Component Highlights
  - "写真をアップロード"
  - "カメラを使用"
  - "写真を撮影"
  - "キャンセル"
  - "あなたの写真"
  - "生成されたキャラクター"
  - "アバターを保存"
  - "画像をダウンロード"
  - "キャラクターのタイプ"
  - "女性"
  - "男性"
  - "写真から生成"
  - "スキップしてかわいいキャラをおまかせ生成"
  - "エラー:"
  - "カメラにアクセスできません。権限を確認してください。"
  - "画像を取得できませんでした。"
  - "画像ファイルの読み込みに失敗しました。"
  - "Gemini APIキーが設定されていません。"
  - "画像生成に失敗しました"
  - "最初に画像をアップロードするか撮影してください。"
  - "画像の形式が正しくありません。"
  - "画像生成中に不明なエラーが発生しました。"

**Supporting Backend (Avatar Upload API)**
```ts
import { createClient } from '@supabase/supabase-js';
import { NextRequest, NextResponse } from 'next/server';

const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.SUPABASE_SERVICE_ROLE_KEY!
);

export async function POST(request: NextRequest) {
  try {
    // Get the user from the session using Supabase auth
    const authHeader = request.headers.get('authorization');
    if (!authHeader || !authHeader.startsWith('Bearer ')) {
      return NextResponse.json(
        { error: 'Unauthorized' },
        { status: 401 }
      );
    }

    const token = authHeader.split(' ')[1];

    // Create a Supabase client with the user's JWT token
    const userSupabase = createClient(
      process.env.NEXT_PUBLIC_SUPABASE_URL!,
      process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
      {
        global: {
          headers: {
            Authorization: `Bearer ${token}`,
          },
        },
      }
    );

    // Verify the user is authenticated
    const { data: { user }, error: authError } = await userSupabase.auth.getUser();

    if (authError || !user) {
      return NextResponse.json(
        { error: 'Invalid token' },
        { status: 401 }
      );
    }

    // Parse the multipart form data
    const formData = await request.formData();
    const file = formData.get('file') as File;
    const avatarConfig = formData.get('avatar_config') as string;

    if (!file) {
      return NextResponse.json(
        { error: 'No file provided' },
        { status: 400 }
      );
    }

    // Validate file type
    if (!file.type.startsWith('image/')) {
      return NextResponse.json(
        { error: 'File must be an image' },
        { status: 400 }
      );
    }

    // Validate file size (2MB limit for avatars)
    if (file.size > 2 * 1024 * 1024) {
      return NextResponse.json(
        { error: 'File size must be less than 2MB' },
        { status: 400 }
      );
    }

    // Generate unique file name
    const fileExt = file.name.split('.').pop() || 'png';
    const fileName = `avatar-${user.id}-${Date.now()}.${fileExt}`;
    const filePath = `avatars/${fileName}`;

    // Upload to Supabase Storage
    const { data: uploadData, error: uploadError } = await supabase.storage
      .from('profile-assets')
      .upload(filePath, file, {
        cacheControl: '3600',
        upsert: true
      });

    if (uploadError) {
      console.error('Upload error:', uploadError);
      return NextResponse.json(
        { error: 'Failed to upload file' },
        { status: 500 }
      );
    }

    // Get public URL
    const { data: { publicUrl } } = supabase.storage
      .from('profile-assets')
      .getPublicUrl(filePath);

    // Update user's profile with new avatar URL and config
    const { error: updateError } = await userSupabase
      .from('user_profiles')
      .upsert({
        user_id: user.id,
        avatar_url: publicUrl,
        avatar_config: avatarConfig ? JSON.parse(avatarConfig) : null,
        updated_at: new Date().toISOString()
      });

    if (updateError) {
      console.error('Profile update error:', updateError);
      return NextResponse.json(
        { error: 'Failed to update profile' },
        { status: 500 }
      );
    }

    return NextResponse.json({
      success: true,
      avatarUrl: publicUrl,
      message: 'アバターが正常にアップロードされました'
    });

  } catch (error) {
    console.error('Avatar upload error:', error);
    return NextResponse.json(
      { error: 'Internal server error' },
      { status: 500 }
    );
  }
}
```

---

## Exchange Center (`/exchange` – `src/app/exchange/page.tsx`)
**Prompt Goal**  
Design the WKP exchange and donation portal. Provide tabs for "WKPを換金" and "WKPを購入", each hosting dynamic forms. Highlight the user’s current OP balance and monthly redemption stats. Include transaction history list beneath the forms and a sticky sidebar with WKP wallet overview plus donation panel. Copy stays in Japanese, guiding users through redeeming OP for JPY, USDT, JPYC, or TEC, or purchasing/donating. Consider states for loading user auth, missing token, and empty transaction list.

**Existing UI Text**
- "WKP 交換・寄付センター"
- "WKPを交換、寄付、またはチップを送信します。"
- "現在のOP残高: 🪙 {op_balance} OP"
- "WKPを換金"
- "WKPを購入"
- Loading copy: "読み込み中..."
- Console error strings: "No auth token found", "Failed to fetch transactions"

---

## Dashboard Home (`/dashboard` – `src/app/dashboard/page.tsx`)
**Prompt Goal**  
Outline the main authenticated feed experience within a ProtectedRoute wrapper. Sections include stories carousel, create-post card with emoji picker and media upload, list of community posts with engagement actions, and fallbacks for loading/error/empty states. Mention fetch flows to /api/profile and /api/posts using auth_token, handling 401 by redirecting to login. UI should feel like a social feed rooted in love and contribution. All user-facing strings in Japanese.

**Existing UI Text**
- Loading/Error States
  - "読み込み中..."
  - "再読み込み"
  - "テキスト: 読み込み中..." (spinner messages)
  - "プロフィールデータを読み込めませんでした。"
- Create Post Card
  - Placeholder: "何を考えていますか、{displayName}さん？"
  - Toasts: "投稿は空にできません", "何かメッセージを入力するか、メディアをアップロードしてください。"
  - Success Toast: "投稿しました！", "あなたの考えが共有されました。"
  - Error Toast: "投稿エラー", "投稿の保存中にエラーが発生しました。"
  - Media actions: "投稿する", "投稿中..."
- Post Card Interactions
  - Buttons: "いいね", "コメント", "シェア", "保存"
  - Toasts: "認証エラー", "ログインしてください。", "エラー", "いいねの処理中にエラーが発生しました。", "保存の処理中にエラーが発生しました。"
  - Share messages: "リンクをコピーしました", "投稿リンクがクリップボードにコピーされました。"
  - Comments: "コメントを追加...", "投稿", "投稿中...", "読み込み中...", "まだコメントがありません。最初のコメントを投稿しましょう！"
  - Comment success toast: "コメントを投稿しました"
  - Comment error toast: "コメントエラー", "コメントの投稿中にエラーが発生しました。"
  - Share error toast: "共有エラー", "投稿の共有中にエラーが発生しました。"
  - Tip button uses text via `TipButton` (not localized here but callout).

---

## Dashboard Discover (`/dashboard/discover`)
**Prompt Goal**  
Describe a grid-based discovery view displaying 21 image tiles sourced from randomized Picsum seeds. Each tile must support hover overlay showing likes and comments with icons (Heart, MessageCircle). Provide page heading "発見" and ensure images include data-ai-hint metadata for compliance. Layout uses responsive three-column grid with subtle gaps.

**Existing UI Text**
- "発見"

---

## Dashboard Marketplace Listing (`/dashboard/marketplace`)
**Prompt Goal**  
Specify an interactive marketplace listing page with client-side filters (search, category select, sort order), live WKP price subscription, and product cards showing image, category badge, like button, seller mini-profile, price in WKP and approximate yen, condition, created date, and quick actions (message, purchase placeholder). Include header CTA to create a new listing via dialog containing CreateAdForm. Provide loading skeleton, empty state, and toast messaging guidance. Emphasize Japanese copy and community-first tone.

**Existing UI Text**
- "マーケットプレイス"
- "コミュニティメンバーが作った素晴らしい商品を発見しましょう"
- Button: "商品を出品する"
- Search placeholder: "商品を検索..."
- Category select: "カテゴリー"
- Sort select: "並び替え", options "新しい順", "価格が低い順", "価格が高い順", "人気順"
- Result count: "{count}件の商品が見つかりました"
- Card actions: "いいね", "メッセージ", "購入"
- Toasts / Messages
  - "ログインが必要です"
  - "いいねするにはログインしてください。"
  - "リンクをコピーしました"
  - "商品リンクをクリップボードにコピーしました。"
  - "メッセージ機能は開発中です"
  - "購入機能は開発中です"
  - "いいねしました", "いいねを解除しました"
  - "いいねに失敗しました"
- Loading/Empty
  - "商品を読み込み中..."
  - "商品が見つかりませんでした。"

---

## Marketplace Detail (`/dashboard/marketplace/[adId]`)
**Prompt Goal**  
Guide the creation of a detailed marketplace product page. Requirements: hero gallery image, seller information, like/share buttons, condition/location metadata, price in WKP + yen estimate, comment thread with posting form, offer dialog workflow (amount + message), edit/delete controls for owner, and fallbacks for loading, missing ad, or auth errors. Maintain integration notes for priceService subscription and API endpoints (/api/marketplace/:id, /comments, /offers). Tone is supportive and transparent.

**Existing UI Text**
- Navigation & Actions
  - "← マーケットプレイスに戻る"
  - "共有"
  - "編集"
  - "削除"
- Product Details
  - Status badges: "販売中", "売却済み", "停止中"
  - "約 ¥{price}"
  - "閲覧数"
  - "いいね"
  - "コメント"
- Offer Dialog
  - "オファーを送る"
  - "オファー金額 (円)"
  - "希望金額を入力"
  - "メッセージ (任意)"
  - "売り手にメッセージを送る"
  - "オファーを送信"
  - "送信中..."
  - "購入を申し込む"
- Comments Section
  - "コメント"
  - "コメントを入力..."
  - "コメントがありません"
- Offers for Seller
  - Status chips: "保留中", "承認済み", "拒否済み"
  - Buttons: "承認", "拒否"
  - Toast placeholders: "オファー承認機能は開発中です", "オファー拒否機能は開発中です"
- Toast Copy (auth)
  - "ログインが必要です"
  - "いいねするにはログインしてください。"
  - Error fallback: "商品の詳細を読み込めませんでした。"

---

## Dashboard Notifications (`/dashboard/notifications`)
**Prompt Goal**  
Produce a notifications hub with card layout, tab-like grouping (unread vs earlier), each notification featuring avatar placeholder, message body, timestamp, and unread indicator dot. Include loading spinner state when fetching /api/notifications. Keep overall layout compact and centered within max-width container, using Japanese headers and copy.

**Existing UI Text**
- "お知らせ"
- "未読"
- "既読"
- Loading text: "読み込み中..."

---

## Dashboard Search (`/dashboard/search`)
**Prompt Goal**  
Create the search results page structure. It should read the query parameter `q`, display a heading confirming the search term, and render SearchResults component inside a card. Provide fallback loading state via Suspense and maintain consistent container padding. Copy should reassure users they can refine their search.

**Existing UI Text**
- "検索結果"
- "「{query}」の検索結果"
- Card title: "検索結果"

---

## Dashboard Profile (`/dashboard/profile`)
**Prompt Goal**  
Detail the member profile view: banner image, avatar, username, follow/message/settings actions, metrics (posts, followers, following, OP balance), biography, and tabbed content for posts, saved, tagged. Posts tab shows grid of images with hover stats. Include loading state, fallback profile generation if API fails, and ensure all UI strings remain Japanese.

**Existing UI Text**
- Loading/Error
  - "読み込み中..."
  - "プロフィールデータを読み込めませんでした。"
- Header
  - Buttons: "フォロー中", "メッセージ"
  - Metrics: "投稿", "フォロワー", "フォロー中"
  - OP balance label: "🪙 {userProfile.op_balance} OP"
- Tabs
  - "投稿"
  - "保存済み"
  - "タグ付けされた投稿"
- Empty States
  - "保存済みの投稿はここに表示されます。"
  - "タグ付けされた投稿はここに表示されます。"

---

## Dashboard Settings (`/dashboard/settings`)
**Prompt Goal**  
Compose a comprehensive settings prompt covering profile editing, banner upload (BannerUpload component), avatar creation dialog embedding KawaiiGenerator with onSave workflow, bio editing, account section with email and password placeholders, notification/privacy toggles (Switch components), and saving feedback via toasts. Mention dependencies on /api/profile GET/PUT, /api/upload/avatar, /api/upload/banner, and authentication via auth_token. Emphasize clean two-column layout with nav links and content cards.

**Existing UI Text**
- Page Structure
  - "設定"
  - Nav links: "プロフィール編集", "アカウント", "通知", "プライバシー"
- Profile Card
  - "プロフィール編集"
  - "公開プロフィール情報を更新します。"
  - "プロフィールバナー"
  - Button: "アバターを作成"
  - Labels: "氏名", "ユーザーネーム", "自己紹介"
  - Button: "プロフィールを保存"
  - Loading copy: "保存中..."
- Account Card
  - "アカウント"
  - "アカウント設定を管理します。"
  - "メールアドレス"
  - "現在のパスワード"
  - "新しいパスワード"
- Toast Copy
  - "バナーを更新しました"
  - "アップロードエラー"
  - "プロフィールを保存しました"
  - "保存エラー"
  - "アバターを保存しました"

---

## Dashboard Wallet (`/dashboard/wallet`)
**Prompt Goal**  
Describe a succinct wallet dashboard featuring heading, supportive copy, and WKPWalletComponent rendering transaction balances, recent activity, and actions. Ensure spacing mirrors rest of dashboard pages and copy stays in Japanese, guiding users to manage Oneness Kingdom Points responsibly.

**Existing UI Text**
- "WKP ウォレット"
- "あなたのOneness Kingdom Pointsを管理しましょう"

---

## Dashboard Reels (`/dashboard/reels`)
**Prompt Goal**  
Define a vertically scrolling reels viewer with dark theme, auto-playing muted videos, overlay gradient, user info (avatar, username, follow button), caption, audio label, and action stack (heart, comment, share, more). Provide placeholder data for two reels using external video URLs. Maintain snap scrolling interactions and Japanese captions/hashtags.

**Existing UI Text**
- Reel Data
  - "ワンネスの心 #平和 #愛"
  - "創造の瞬間 ✨ #アート"
  - "オリジナルオーディオ - クリエイター1"
  - "オリジナルオーディオ - アートの魂"
- Overlay Actions
  - "フォロー"

---

## Test Auth (`/test-auth` – `src/app/test-auth/page.tsx`)
**Prompt Goal**  
Outline a simple diagnostic page that reports authentication status, shows current user info if logged in, and provides login/logout buttons. Emphasize usage of useAuth hook, logout helper, and navigation to /login or landing page. Keep UI minimal, developer-focused, yet translated strings where visible.

**Existing UI Text**
- "Authentication Test"
- "Authentication Status:"
- "Logged In"
- "Not Logged In"
- "User:"
- "Loading..."
- "Logout"
- "Login"
